name: Release

on: push
  # workflow_run:
  #   workflows: [Build]
  #   types: [completed]
  #   tags: ["v*"]

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  release:
    # if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
      # TODO: add version to binary name
      - name: Build static binaries
        run: |
          GOOS=darwin GOARCH=amd64 go build -a -ldflags '-extldflags "-static"' -o iecs-darwin-amd64
          GOOS=darwin GOARCH=arm64 go build -a -ldflags '-extldflags "-static"' -o iecs-darwin-arm64
          GOOS=linux GOARCH=amd64 go build -a -ldflags '-extldflags "-static"' -o iecs-linux-amd64
        env:
          CGO_ENABLED: 0
      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            iecs-darwin-amd64
            iecs-darwin-arm64
            iecs-linux-amd64
          generate_release_notes: true
          draft: true
